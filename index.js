const secret = "Williams watches porn";

const firstname = "Williams";
const surname = "Olabamidele";

const greetings = (name) => {
  console.log(`Hello ${name}`);
};

greetings(names.firstname);
greetings(names.surname);

module.exports = { firstname, surname };
